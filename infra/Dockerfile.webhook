FROM python:3.12-alpine

# docker CLI + compose plugin + git + bash
RUN apk add --no-cache docker-cli docker-cli-compose git bash openssh-client

WORKDIR /app
COPY scripts/webhook.py /app/webhook.py
COPY scripts/deploy.sh /app/deploy.sh
RUN chmod +x /app/deploy.sh

CMD ["python3", "/app/webhook.py"]
